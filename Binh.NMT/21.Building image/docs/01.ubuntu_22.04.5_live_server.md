# Build image Ubuntu 22.04.5 live server bằng KVM
# 1. Chuẩn bị
- Công cụ hỗ trợ hỗ trợ đóng image: máy chủ KVM sử dụng dashboard webvirt 
- File iso: https://ubuntu.com/download/server/thank-you?version=24.04.4&architecture=amd64&lts=true

# 2. Các bước thực hiện đóng image
## 2.1 Khởi tạo máy ảo trên KVM
### 2.1.1 Khởi tạo ổ cứng cài đặt OS
- Khởi tạo disk theo các bước sau:

  ![](/imgs/1.png)

  ![](/imgs/2.png)

  ![](/imgs/3.png)

- Đặt tên disk:

  ![](/imgs/4.png)

### 2.1.2 Thực hiện khởi tạo VM: 

- Khởi tạo VM: 

   ![](/imgs/5.png)

- Thực hiện lựa chọn Compute khởi tạo máy ảo sau đó bấm next: 

  ![](/imgs/6.png)

  ![](/imgs/7.png)

- Chọn Custom sau đó thực hiện khởi tạo máy ảo:

  ![](/imgs/8.png)

### 2.1.3 Mount file ISO
- Chọn Settings và chọn Disk:

  ![](/imgs/9.png)

- Tại CD-ROM 1 tiến hành chọn iso đã download và thực hiện mount:

  ![](/imgs/10.png)

- Thực hiện chỉnh sửa disk boot vào iso:

  ![](/imgs/11.png)

- Khởi tạo Snapshot **no-os** trước khi cài đặt OS:

  ![](/imgs/12.png)


## 2.2 Cài đặt OS
- **Bước 1: Bật máy ảo và truy cập console**

  ![](/imgs/13.png)

  ![](/imgs/14.png)

- **Bước 2: Cài đặt OS**
  - Chọn English:

  ![](/imgs/15.png)
  
  - Chọn Continue without updating:

  ![](/imgs/16.png)
  
  - Chọn Done:

  ![](/imgs/17.png)
  
  - Chọn Done:

  ![](/imgs/18.png)
  
  - Chọn Done:

  ![](/imgs/19.png)
  
  - Chọn Done:

  ![](/imgs/20.png)
  
  - Chọn Done:
  
  ![](/imgs/21.png)
  
  - Chọn Use an entire disk > Done. Lưu ý: KHÔNG DÙNG OPTION THIẾT LẬP DISK dạng LVM:

  ![](/imgs/22.png)
  
  - Chọn Done:

  ![](/imgs/23.png)
  
  - Điền các thông tin cho máy ảo. User mặc định được sử dụng là cloud:

  ![](/imgs/24.png)
  
  - Bỏ qua các option, kéo xuống chọn Done:

  ![](/imgs/25.png)
  
  - Chọn Continue để tiếp tục:

  ![](/imgs/26.png)
  
  - Chọn cài đặt Install OpenSSH Server:

  ![](/imgs/27.png)
  
  - Quá trình cài đặt Ubuntu 22.04 bắt đầu. Chọn Cancel update and reboot, lưu ý ĐỢI VM REBOOT VÀO ĐƯỢC OS RỒI MỚI THỰC HIỆN BƯỚC TIẾP THEO:

  ![](/imgs/28.png)

- **Bước 3: Tắt VM, chỉnh lại BOOT OPTION**
  
  ![](/imgs/29.png)

- **Bước 4: Truy cập VM, kiểm tra dịch vụ SSH**
  
  - Lưu ý nếu dịch vụ SSH không khởi động được, thực hiện:
  
  ```
  sudo ssh-keygen -A
  sudo systemctl restart sshd
  ```
- **Bước 5: Tắt VM, tạo Snapshot PreSetupOS**

  ![](/imgs/30.png)

- **Bước 6: Chỉnh sửa file XML VM**
  
  - Lưu ý: Chỉnh sửa file .xml của máy ảo, bổ sung thêm channel trong (để máy host giao tiếp với máy ảo sử dụng qemu-guest-agent), sau đó save lại.

  - Truy cập Settings > XML > EDIT SETTINGS:

  ```
  <devices>
  <channel type='unix'>
    <target type='virtio' name='org.qemu.guest_agent.0'/>
    <address type='virtio-serial' controller='0' bus='0' port='1'/>
  </channel>
  </devices>
  ```

- **Bước 7: Thực hiện snapshot OS mới**
  
  ![](/imgs/31.png)

  Sau khi hoàn thành thực hiện snapshot thực hiện bật máy ảo để cấu hình thiết lập cơ bản
## 2.3 Thiết lập môi trường
### 2.3.1 Thiết lập môi trường cơ bản
- **Bước 1: Thiết lập SSH**
  
  - Truy cập user root:
  ```
  sudo su
  ```

  - Đặt mật khẩu cho root:
  ```
  passwd
  ```

  - Cấu hình cho phép ssh bằng user root /etc/ssh/sshd_config:
  ```
  sed -i 's/#ListenAddress 0.0.0.0/ListenAddress 0.0.0.0/'g /etc/ssh/sshd_config

  sed -i 's/#PermitRootLogin prohibit-password/PermitRootLogin yes/'g /etc/ssh/sshd_config

  sed -i 's/#PubkeyAuthentication yes/PubkeyAuthentication yes/'g /etc/ssh/sshd_config

  sed -i 's/#PasswordAuthentication yes/PasswordAuthentication yes/'g /etc/ssh/sshd_config
  
  sed -i 's/#AuthorizedKeysFile/AuthorizedKeysFile/'g /etc/ssh/sshd_config

  systemctl restart sshd
  ```

  - Disable firewalld:
  ```
  systemctl disable ufw

  systemctl stop ufw

  systemctl status ufw
  ```

- **Bước 2: Điều chỉnh timezone**

  - Đổi timezone về Asia/Ho_Chi_Minh:
  ```
  timedatectl set-timezone Asia/Ho_Chi_Minh
  ```

  - Bổ sung env locale:
  ```
  echo "export LC_ALL=C" >>  ~/.bashrc
  ```

- **Bước 3: Enable IPv6**

  - Thực hiện:
  ```
  echo "net.ipv6.conf.all.disable_ipv6 = 0" >> /etc/sysctl.conf 

  echo "net.ipv6.conf.default.disable_ipv6 = 0" >> /etc/sysctl.conf 

  echo "net.ipv6.conf.lo.disable_ipv6 = 0" >> /etc/sysctl.conf

  sysctl -p
  ```

  - Kiểm tra:
  ```
  cat /proc/sys/net/ipv6/conf/all/disable_ipv6
  ```

- **Bước 4: Cập nhật gói, update OS**

   ```
   apt-get update -y 
   apt-get upgrade -y 
   apt-get dist-upgrade -y
   apt-get autoremove 
   sudo apt install nfs-common net-tools wget vim -y
   ```

- **Bước 5: Thực hiện reboot và xóa user cloud**
   - Tắt VM thực hiện snapshot:
   ```
   init 0
   ```

   - Tạo snapshot **basic**:

   - Login lại bằng user root và xóa user cloud:
   ```
   userdel cloud
   rm -rf /home/cloud
   ```

- **Bước 6: Cấu hình để instance báo log ra console, đổi tên Card mạng về eth* thay vì ens, eno**
   ```
   sed -i 's|GRUB_CMDLINE_LINUX=""|GRUB_CMDLINE_LINUX="net.ifnames=0 biosdevname=0 console=tty1 console=ttyS0"|g' /etc/default/grub

   update-grub
   ```

- **Bước 7: Kiểm tra định dạng disk os**
   
   - Chỉ định disk cài đặt OS sử dụng định dạng GPT.
   - Sử dụng parted để kiểm tra định dạng disk hiện tại.
   ```
   sudo parted -l
   ```

- Bước 8: Tắt VM, tạo snapshot **OSBegin**
### 2.3.2 Thiết lập dịch vụ cơ bản cho template
- **Bước 1: Cài đặt banner VNPT Cloud: chỉ thực hiện được trên Ubuntu 24.04**
  
   - Tạo file:
   ```
   vi /etc/update-motd.d/93-custom-banner
   ```

   - Copy nội dung vào file:
   ```
   #!/bin/sh

   printf "\033[0;34m .--.   ,--..--.  .--..------. .--------.     .-----..--.                  .--. \033[0m\n"
   printf "\033[0;34m  \\  \`.'  / |  .'.|  ||  .--. ''--.  .--'    '  .--.||  | ,---. .--..--. ,-|  | \033[0m\n"
   printf "\033[0;34m   \\     /  |  |' '  ||  '--' |   |  |       |  |    |  || .-. ||  ||  |' .-. | \033[0m\n"
   printf "\033[0;34m    \\   /   |  | \`   ||  | --'    |  |       '  '--'||  |' '-' ''  ''  '\\ \`-' | \033[0m\n"
   printf "\033[0;34m     \`-'    \`--'  \`--'\`--'        \`--'        \`-----'\`--' \`---'  \`----'  \`---'' \033[0m\n"
   printf "\n"
   printf "\033[0;34m * VNPT Cloud - VNPT\033[0m\n"
   printf "\033[0;34m   Website: https://cloud.vnpt.vn\033[0m\n"
   printf "\033[0;34m   Docs: https://cloud.vnpt.vn/tai-lieu\033[0m\n"
   ```

   - Phân quyền:
   ```
   chmod +x /etc/update-motd.d/93-custom-banner
   ```

   - Test thử:
   ```
   run-parts /etc/update-motd.d/
   ```

- **Bước 2: Thiết lập gói cloud-init**
  
  - Xóa file cấu hình cloud-init cũ:
  ```
  sudo apt purge cloud-init -y
  rm -rf /etc/cloud/
  rm -rf /var/lib/cloud/
  rm -f /var/log/cloud-init.log
  rm -f /var/log/cloud-init-output.log
  ```

  - Cài đặt cloud-init:
  ```
  apt-get install -y cloud-init
  ```

  - Cấu hình user mặc định:
  ```
  sed -i 's/name: ubuntu/name: root/g' /etc/cloud/cloud.cfg
  sed -i 's/^disable_root: true$/disable_root: false/' /etc/cloud/cloud.cfg
  ```

  - Disable default config route:
  ```
  sed -i 's|link-local 169.254.0.0|#link-local 169.254.0.0|g' /etc/networks
  ```
  
  - Cấu hình datasource, bỏ chọn mục NoCloud bằng cách dùng dấu SPACE, sau đó ấn ENTER:
  ```
  dpkg-reconfigure cloud-init
  ```
- **Bước 3: Cài đặt qemu-agent**

  Chú ý: 
  - qemu-guest-agent là một daemon chạy trong máy ảo, giúp quản lý và hỗ trợ máy ảo khi cần (có thể cân nhắc việc cài thành phần này lên máy ảo). 
  - Để có thể thay đổi password máy ảo bằng nova-set password thì phiên bản qemu-guest-agent phải >= 2.5.0
  
  ```
  apt-get install software-properties-common -y
  apt-get update -y
  apt-get install qemu-guest-agent -y
  systemctl enable --now qemu-guest-agent
  ```
  
  - Kiểm tra phiên bản qemu-ga bằng lệnh:
  ```
  qemu-ga --version
  systemctl status qemu-guest-agent
  ```

- **Bước 4: Cài đặt CMDlog và welcome Display**
  - Cài đặt CMDlog:
  ```
  apt-get -y install rsyslog

  touch /var/log/cmdlog.log

  chmod 600 /var/log/cmdlog.log

  echo "# Command log" >> ~/.bashrc

  echo "export PROMPT_COMMAND='RETRN_VAL=$?;logger -p local6.debug \"[\$(echo \$SSH_CLIENT | cut -d\" \" -f1)] # \$(history 1 | sed \"s/^[ ]*[0-9]\+[ ]*//\" )\"'" >> ~/.bashrc

  echo 'export HISTTIMEFORMAT="%d/%m/%y %T "' >> ~/.bashrc

  source ~/.bashrc

  mv /etc/default/{useradd,useradd.bk}
  ```

  ```
  echo """# useradd defaults file
  GROUP=100
  HOME=/home
  INACTIVE=-1
  EXPIRE=
  SHELL=/bin/sh
  SKEL=/etc/skel
  CREATE_MAIL_SPOOL=yes""" > /etc/default/useradd
  ```

  ```
  mkdir -p /etc/skel

  curl -o /etc/skel/.bashrc  https://s3hn.smartcloud.vn/vnptcloud-images/file-config-image/cmdlog/Ubuntu.bashrc > /dev/null 2>&1

  curl -o /etc/skel/.profile  https://s3hn.smartcloud.vn/vnptcloud-images/file-config-image/cmdlog/Ubuntu.profile > /dev/null 2>&1
  ```

  - Chỉnh sửa nội dung file: /etc/rsyslog.conf:
  ```
  curl -o /etc/rsyslog.conf https://s3hn.smartcloud.vn/vnptcloud-images/file-config-image/cmdlog/Ubuntu24_rsyslog.cnf > /dev/null 2>&1
  ```

  - Khởi động lại dịch vụ rsyslog:
  ```
  systemctl restart rsyslog.service
  source ~/.bashrc
  ```

  - Log out rồi login lại kiểm tra: cat /var/log/cmdlog.log:
  
  - Thực hiện tắt máy ảo và và tạo Snapshot: **Image-basic**
- **Bước 5: Dọn dẹp log**

   - Clear toàn bộ history:
   ```
   apt-get clean all
   rm -f /var/log/wtmp /var/log/btmp
   > /var/log/cmdlog.log
   > .bash_history
   history -c
   ```

   - Clean cấu hình và restart service:
   ```
   cloud-init clean
   systemctl restart cloud-init
   systemctl enable cloud-init
   systemctl status cloud-init
   ```
   - Tắt máy ảo:
   ```
   init 0
   ```
# 3. Tối ưu image: Thực hiện trên máy chủ KVM
- Bước 1: Sử dụng lệnh virt-sysprep để xóa toàn bộ các thông tin máy ảo

  ```
  virt-sysprep -d ubuntu22.04.5-live-server-template
  ```

- Bước 2: Tối ưu kích thước image
   
   ```
   virt-sparsify --compress --convert qcow2 /kvm/binhnmt-ubuntu22.04.5-live-server.qcow2 Ubuntu22.04.5_QEMU_x86_64_docker.qcow2
   ```
# 4. Upload image lên Openstack: Thực hiện trên node controller

- Bước 1: Copy file image sang node Controller

  ```
  scp Ubuntu22.04.5_QEMU_x86_64_docker.qcow2 root@172.16.68.11:/root/
  ```

- Bước 2: Convert image về định dạng raw

  ```
  qemu-img convert -f qcow2 -O raw  Ubuntu22.04.5_QEMU_x86_64_docker.qcow2 Ubuntu22.04.5_QEMU_x86_64_docker.raw
  ```

- Bước 3: Đẩy image lên hệ thống và sử dụng

   ```
   glance image-create --name "Ubuntu22.04.5_QEMU-64bits_docker" \
  --file Ubuntu22.04.5_QEMU_x86_64_docker.raw \
  --disk-format raw \
  --container-format bare \
  --visibility public \
  --property os_type=linux \
  --property hw_qemu_guest_agent=yes \
  --min-disk 10 \
  --min-ram 1024 \
  --progress
   ```